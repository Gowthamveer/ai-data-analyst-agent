[project]
name = "ai-data-analyst-agent"
version = "0.1.0"
description = "AI Data Analyst Agent for OpenEnv"
authors = [{name = "Your Name"}]
dependencies = [
    "fastapi",
    "uvicorn",
    "pandas",
    "numpy",
    "pydantic",
    "scikit-learn"
]
